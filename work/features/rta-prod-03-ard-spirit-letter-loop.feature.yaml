kind: Feature
id: rta-prod-03-ard-spirit-letter-loop
name: RtaProd03ArdSpiritLetterLoop
status: demo-covered
why: ARDs should enforce the spirit/letter loop so architectural claims are backed by reciprocal checks.
ownedBy:
  internal: rta-platform
demonstratedBy:
  - command: rta check --ard-meta
  - fixture: bad ARD family fails
qaSteps:
  - do: node scripts/rta.mjs check --ard-meta
  - do: run node --test tests/cli.test.mjs
  - see: RtaProd03ArdSpiritLetterLoop produces visible evidence or a successful check result
requires:
  - VocabArdCliSkeleton
produces:
  - reciprocal spirit/letter validation
  - ARD family id enforcement
  - check-backed letter ARDs
selfAudit:
  - "@did every spirit and letter ARD reciprocate and point to a concrete check?"
