kind: Feature
id: rta-prod-07-usecase-scenario-boundary
name: RtaProd07UsecaseScenarioBoundary
status: demo-covered
why: User stories, use cases, scenarios, inter-bounded-context tests, and integration tests must be first-class and enforceable.
ownedBy:
  internal: rta-platform
demonstratedBy:
  - command: rta check --use-cases
  - command: rta check --scenario-coverage
  - command: rta check --boundary-coverage
qaSteps:
  - run: node scripts/rta.mjs check --use-cases
  - run: node scripts/rta.mjs check --scenario-coverage
  - run: node scripts/rta.mjs check --boundary-coverage
  - run: node scripts/rta.mjs scenario list
requires:
  - UseCaseScenarioCoverage
produces:
  - use-case schema
  - executable scenario schema
  - boundary coverage and integration checks
selfAudit:
  - "@did every use case and bounded-context connection have an executable proving scenario?"
